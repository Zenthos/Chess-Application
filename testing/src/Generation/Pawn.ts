import { Occupancies } from '../Types';

// Generates which tiles a Pawn can attack from the given square
export function generatePawnAttacksMask(square: number) {

}

// Generate all the moves a Pawn can do, depending on the side to move
export function generatePawnMoves(occupancies: Occupancies, side: keyof Occupancies, capturesOnly = false) {

}
