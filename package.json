{
  "name": "chess-application",
  "license": "MIT",
  "scripts": {
    "build:client": "cd client && yarn install && yarn build",
    "build:server": "cd server && yarn install && yarn build",
    "start": "yarn build:client && yarn build:server && cd server && yarn start"
  }
}